<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Internet (API calls, maps, etc.) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Location (GPS tracking) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>
    <!-- Audio recording (silent recording, voice trigger) -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Camera (video evidence capture) -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Offline SMS alerts -->
    <uses-permission android:name="android.permission.SEND_SMS" />

    <!-- Haptics / SOS vibration -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Optional but helpful for some recording/background behaviors -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Optional: if you later add background location tracking -->
    <!--
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    -->

    <application
        android:allowBackup="true"
        android:label="aawaj_lastt"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <meta-data android:name="com.google.android.geo.API_KEY"
                android:value="YOUR KEY HERE"/>

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

        </activity>

        <!-- Flutter embedding meta-data -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

    </application>

    <!-- Required to query activities that can process text (Flutter ProcessTextPlugin) -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>

</manifest>